name: CI — realsass-sass-back

on:
  push:
    paths:
      - 'realsass-sass-back/**'
      - 'packages/**'
      - 'pnpm-workspace.yaml'
      - 'package.json'
  pull_request:
    branches: [main]
    paths:
      - 'realsass-sass-back/**'
      - 'packages/**'

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: '10' }
      - uses: actions/setup-node@v4
        with: { node-version: '22', cache: 'pnpm' }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter realsass-sass-back typecheck
      - run: pnpm --filter realsass-sass-back lint
      - run: pnpm --filter realsass-sass-back test:cov
        env: { CI: true }
      - run: pnpm --filter realsass-sass-back build
